import {useEffect,useState} from "react";import {getMySalary} from "../../api/salaryApi";import Loading from "../../components/Loading";
export default function Salary(){const[data,setData]=useState(null);useEffect(()=>{getMySalary().then(setData).catch(console.error)},[]);if(!data)return <Loading/>;const s=data.salary,c=data.calculation||{};return <div className="space-y-5"><h1 className="text-3xl font-bold">My Salary</h1><div className="grid gap-4 md:grid-cols-4">{[["Basic",c.basicSalary??s.basicSalary],["Allowances",c.totalAllowances],["Gross",c.grossSalary],["Net",c.netSalary]].map(([t,v])=><div key={t} className="rounded-2xl border bg-white p-5"><p className="text-sm text-slate-500">{t}</p><p className="mt-2 text-2xl font-bold">₹{Number(v||0).toLocaleString()}</p></div>)}</div><div className="grid gap-5 md:grid-cols-2">{[["Allowances",s.allowances],["Deductions",s.deductions]].map(([title,rows=[]])=><div key={title} className="rounded-2xl border bg-white p-6"><h2 className="font-semibold">{title}</h2><div className="mt-4 space-y-3">{rows?.length?rows.map((r,i)=><div key={i} className="flex justify-between border-b pb-2 text-sm"><span>{r.name}</span><span>{r.type==="PERCENTAGE"?`${r.value}%`:`₹${r.value}`}</span></div>):<p className="text-sm text-slate-500">None</p>}</div></div>)}</div></div>}