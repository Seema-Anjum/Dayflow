import {useEffect,useState} from "react";import {getMyPayroll} from "../../api/payrollApi";import Loading from "../../components/Loading";import Badge from "../../components/Badge";
export default function Payroll(){const[items,setItems]=useState([]),[loading,setLoading]=useState(true);useEffect(()=>{getMyPayroll().then(d=>setItems(d.payroll||[])).catch(console.error).finally(()=>setLoading(false))},[]);if(loading)return <Loading/>;return <div className="space-y-5"><h1 className="text-3xl font-bold">My Payroll</h1>{!items.length?<div className="rounded-2xl border bg-white p-10 text-center text-sm text-slate-500">No payroll records.</div>:<div className="overflow-x-auto rounded-2xl border bg-white"><table><thead className="bg-slate-50 text-left text-xs text-slate-500"><tr><th className="px-5 py-3">Period</th><th>Working</th><th>Present</th><th>Leave</th><th>Gross</th><th>Net</th><th>Status</th></tr></thead><tbody className="text-sm">{items.map(x=><tr key={x._id} className="border-t"><td className="px-5 py-4">{x.month}/{x.year}</td><td>{x.workingDays}</td><td>{x.presentDays}</td><td>{x.leaveDays}</td><td>₹{Number(x.grossSalary||0).toLocaleString()}</td><td>₹{Number(x.netSalary||0).toLocaleString()}</td><td><Badge>{x.status}</Badge></td></tr>)}</tbody></table></div>}</div>}