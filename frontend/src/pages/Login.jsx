import { useState } from "react";
import { useNavigate } from "react-router-dom";
import { ArrowRight, LockKeyhole, UserRound } from "lucide-react";
import { loginUser } from "../api/authApi";
import { useAuth } from "../context/AuthContext";

export default function Login(){const navigate=useNavigate();const{login}=useAuth();const[form,setForm]=useState({loginId:"",password:""});const[error,setError]=useState("");const[loading,setLoading]=useState(false);const handleSubmit=async e=>{e.preventDefault();try{setError("");setLoading(true);const data=await loginUser(form);login(data);navigate(["ADMIN","HR"].includes(data.user.role)?"/admin/dashboard":"/employee/dashboard")}catch(error){setError(error.response?.data?.message||"We couldn't sign you in with those details.")}finally{setLoading(false)}};return <main className="grid min-h-screen bg-cloud-50 lg:grid-cols-2"><section className="relative hidden overflow-hidden bg-[#25215b] p-14 text-white lg:flex lg:flex-col"><div className="brand-mark text-white before:shadow-[0_0_0_4px_rgba(255,255,255,.14)]">Dayflow</div><div className="relative z-10 my-auto max-w-lg"><p className="mb-5 text-sm font-bold tracking-[.2em] text-amber-300">WORK, IN FLOW</p><h1 className="text-5xl font-extrabold leading-[1.08]">The clearer way to run your workday.</h1><p className="mt-6 max-w-md text-lg leading-8 text-indigo-100">One calm place for attendance, leave, payroll, and the people who make it all happen.</p></div><div className="absolute -bottom-28 -right-16 size-96 rounded-full bg-violet-500/40 blur-3xl"/><div className="absolute right-24 top-32 size-20 rounded-full border border-white/20"/></section><section className="flex items-center justify-center p-6 sm:p-10"><div className="w-full max-w-md"><div className="mb-12 lg:hidden"><div className="brand-mark">Dayflow</div></div><p className="text-sm font-bold tracking-[.14em] text-brand-600">WELCOME BACK</p><h1 className="mt-3 text-4xl font-extrabold">Sign in to Dayflow</h1><p className="mt-3 text-slate-500">Enter your details to access your workspace.</p>{error&&<div role="alert" className="mt-7 rounded-xl border border-rose-100 bg-rose-50 px-4 py-3 text-sm font-medium text-rose-700">{error}</div>}<form onSubmit={handleSubmit} className="mt-8 space-y-5"><label><span className="field-label">Login ID</span><div className="relative"><UserRound className="absolute left-3 top-3 text-slate-400" size={18}/><input required autoComplete="username" className="input pl-10" placeholder="Enter your login ID" value={form.loginId} onChange={e=>setForm({...form,loginId:e.target.value})}/></div></label><label><span className="field-label">Password</span><div className="relative"><LockKeyhole className="absolute left-3 top-3 text-slate-400" size={18}/><input required type="password" autoComplete="current-password" className="input pl-10" placeholder="Enter your password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></div></label><button disabled={loading} className="btn-primary mt-2 w-full">{loading?"Signing you in…":<>Sign in <ArrowRight size={17}/></>}</button></form></div></section></main>}
