import {useEffect,useState} from "react";
import {Users,UserCheck,Palmtree,Clock3,Wallet} from "lucide-react";
import StatCard from "../../components/StatCard";
import Loading from "../../components/Loading";
import {getAdminDashboard} from "../../api/dashboardApi";
const money=value=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(value||0);
export default function Dashboard(){const[data,setData]=useState(null);const[error,setError]=useState("");useEffect(()=>{getAdminDashboard().then(setData).catch(e=>setError(e.response?.data?.message||"Unable to load the dashboard."))},[]);if(!data&&!error)return <Loading/>;if(error)return <div className="card p-6 text-sm text-rose-700">{error}</div>;const{employees,attendance,leaves,payroll}=data;return <div className="space-y-7"><div className="flex flex-col gap-2 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-bold tracking-wide text-brand-600">OVERVIEW</p><h1 className="mt-1 text-3xl font-extrabold">Good morning</h1><p className="mt-1 text-slate-500">Here’s how your organisation is moving today.</p></div><div className="inline-flex w-fit items-center gap-2 rounded-xl bg-white px-3 py-2 text-sm font-semibold text-slate-500 shadow-sm"><Clock3 size={16} className="text-brand-500"/>{new Date().toLocaleDateString(undefined,{weekday:"long",month:"short",day:"numeric"})}</div></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard title="Total employees" value={employees.total} description="People in your team" icon={Users}/><StatCard title="Present today" value={attendance.presentToday} description="Checked in and working" icon={UserCheck}/><StatCard title="On leave" value={attendance.leaveToday} description="Away from the office" icon={Palmtree}/><StatCard title="Pending requests" value={leaves.pending} description="Awaiting your review" icon={Clock3}/></div><div className="grid gap-5 lg:grid-cols-5"><section className="card p-6 lg:col-span-3"><div className="flex items-center justify-between"><div><h2 className="text-lg font-bold">Team availability</h2><p className="mt-1 text-sm text-slate-500">A quick view of your workforce today.</p></div><Users className="text-brand-500" size={21}/></div><div className="mt-8 grid grid-cols-2 gap-4"><div className="rounded-xl bg-emerald-50 p-4"><p className="text-sm font-semibold text-emerald-700">Active employees</p><p className="mt-2 text-3xl font-extrabold text-emerald-900">{employees.active}</p></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-sm font-semibold text-slate-500">Inactive employees</p><p className="mt-2 text-3xl font-extrabold">{employees.inactive}</p></div></div></section><section className="card p-6 lg:col-span-2"><div className="flex items-center gap-2"><span className="grid size-9 place-items-center rounded-xl bg-brand-50 text-brand-600"><Wallet size={18}/></span><div><h2 className="text-lg font-bold">Payroll snapshot</h2><p className="text-sm text-slate-500">Current payroll totals</p></div></div><dl className="mt-5 space-y-3 text-sm"><div className="flex justify-between border-b border-hairline pb-3"><dt className="text-slate-500">Employees</dt><dd className="font-bold">{payroll.totalEmployees}</dd></div><div className="flex justify-between border-b border-hairline pb-3"><dt className="text-slate-500">Gross salary</dt><dd className="font-bold">{money(payroll.totalGrossSalary)}</dd></div><div className="flex justify-between"><dt className="text-slate-500">Net salary</dt><dd className="font-bold text-brand-600">{money(payroll.totalNetSalary)}</dd></div></dl></section></div></div>}
